To generate the input data use:
	make datagen.c

To run the serialtester to ensure proper operation use:
	make serialtester.c

To compile the serial version of our code use:
	make lab3_serial.c

To compile the parallel version of our code use:
	make lab3_openMP

NOTE: running the default "$make" will generate the parallel executable under the name "main"
